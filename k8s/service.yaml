apiVersion: v1
kind: Service
metadata:
  name: employee-churn-api
  namespace: churn-prediction
  labels:
    app.kubernetes.io/name: employee-churn-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: employee-churn-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
